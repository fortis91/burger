<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{burger_id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Eat Burger{{else}}Make Again!{{/if}}
	</button>
	{{#if devoured}}
	<button class="delete-burger" data-id="{{this.burger_id}}">Trash</button>
	{{/if}}
</li>
